# ========================================
# Microsoft Azure 认证配置
# ========================================
# Azure Active Directory 租户 ID
# 格式: GUID (xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx)
# 获取方式: Azure Portal -> Azure Active Directory -> 概述 -> 租户 ID
TENANT_ID=your_tenant_id_here

# Azure 应用程序 (客户端) ID
# 格式: GUID (xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx)
# 获取方式: Azure Portal -> Azure Active Directory -> 应用注册 -> 选择应用 -> 概览 -> 应用程序(客户端) ID
CLIENT_ID=your_client_id_here

# Azure 应用程序客户端密钥
# 格式: 字符串
# 获取方式: Azure Portal -> Azure Active Directory -> 应用注册 -> 选择应用 -> 证书和密钥 -> 新建客户端密钥
CLIENT_SECRET=your_client_secret_here

# ========================================
# 邮件SMTP配置
# ========================================
# SMTP服务器地址
# 示例: smtp.88.com, smtp.gmail.com, smtp.qq.com
SMTP_SERVER=your_smtp_server_here

# SMTP服务器端口
# 常见值: 465 (SSL), 587 (TLS), 25 (无加密)
SMTP_PORT=465

# 发件人邮箱地址
# 格式: 标准邮箱地址 (user@domain.com)
SENDER_EMAIL=your_sender_email_here

# 发件人邮箱密码或授权码
# 注意: 对于某些邮箱服务(如QQ邮箱、163邮箱等)，需要使用授权码而非登录密码
SENDER_PASSWORD=your_sender_password_here

# ========================================
# 请求配置
# ========================================
# 是否验证SSL证书
# 值: True (推荐) 或 False (仅用于测试环境)
# 注意: 生产环境应始终设置为 True
VERIFY_SSL=True

# 请求超时时间(秒)
# 建议值: 30-60
REQUEST_TIMEOUT=30

# 最大重试次数
# 建议值: 3-5
MAX_RETRIES=3

# ========================================
# 文件路径配置
# ========================================
# 默认输出目录
# Windows示例: C:\path\to\your\output\directory
# Linux/Mac示例: /path/to/your/output/directory
DEFAULT_OUTPUT_DIR=your_output_directory_here

# JSON输出文件名
JSON_FILENAME=e5_sub.json

# 邮件发送记录文件名
EMAIL_LOG_FILE=email_sent_log.json

# 用户配置文件名
USERS_CONFIG_FILE=users.json
